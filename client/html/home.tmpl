{{ define "content" }}
<main>
    <h1>dbuggen</h1>
    {{ range .issues }}
        <a href={{.Name}}>
            <img src={{.Coverimage}}>
            <h3>{{.Name}}</h3>
            <p>{{.Date}}</p>
            <br>
        </a>
        <br>
    {{ end }}
</main>
{{ end }}
