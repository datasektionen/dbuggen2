<!DOCTYPE html>
<body>
    {{template "index" .}}
    <main>
        <h1>dbuggen</h1>
        {{ range .issues }}
        <a href={{.IssueID}}>
            <!-- <img src={{.Coverpage}}> -->
            {{.Coverpage}}
            <h3>{{.Title}}</h3>
            <p>Released on {{.PublishingDate}}. {{.Views}} views.</p>
            <br>
        </a>
        <br>
        {{ end }}
    </main>
</body>
