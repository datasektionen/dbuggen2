<!DOCTYPE html>
<body>
    {{template "index" .}}
    <main>
        <h1>{{.title}}</h1>
        <h2>{{.authors}}</h2>
        <div class="articleContent">
            {{.articleContent}}
        </div>
    </main>
</body>
